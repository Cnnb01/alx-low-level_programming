#include "main.h"
#include <stdlib.h>
/**
*_abs-computes the absolute value of an integer.
*@r: intger being computed
*
*Description: function that computes the absolute value of an integer.
*Return: returns the absolute value of the integer
*/
int _abs(int r)
{
return (abs(r));
}
